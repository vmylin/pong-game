// settings.js
export const SVG_NS = "http://www.w3.org/2000/svg"; // a reference to the game element DOM node in the constructor for our Game
export const KEYS = {
    w: 'w',        // player 1 up key
    s: 's',        // player 1 down key
    up: 'ArrowUp',       // player 2 up key
    down: 'ArrowDown',     // player 2 down key
    spaceBar: ' ', // we'll use this later...
};
